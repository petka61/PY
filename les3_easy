# Задание - 1
# Создайте функцию, принимающую на вход Имя, возраст и город проживания человека
# Функция должна возвращать строку вида "Василий, 21 год(а), проживает в городе Москва"

def user_input():
    name, age, origin_city = input('Введите через запятую ваше имя, возраст и город проживания: ').split (',')
    output = print('{0}, {1} год(а), проживает в городе {2}'.format(name, age, origin_city))
    return output
user_input()

# Задание - 2
# Создайте функцию, принимающую на вход 3 числа, и возвращающую наибольшее из них

def maxi(a, b, c):
    max_num = print(max(a,b,c))
    return max_num
maxi(2, 15, 76)

# Задание - 3
# Создайте функцию, принимающую неограниченное количество строковых аргументов,
# верните самую длинную строку из полученных аргументов

def long_string(*args):
    longest = print(max(*args))
    return longest
long_string('abra', 'abraca', 'abracadabra')
